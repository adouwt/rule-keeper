# AGENTS.md — 项目核心设计契约

> 此文件是项目核心设计的唯一真相源。任何 AI agent 在执行任务前必须先读完此文件。
> 维护协议:见 §6 元规则。维护 skill:`rule-keeper`。
> 最后更新:YYYY-MM-DD
> maxLines: 150
> 适用范围:本仓库所有代码与 AI 协作流程

## 1. 项目指纹

- **项目名**:
- **目标**(一句话):
- **技术栈**:
- **仓库**:

## 2. 架构骨架

**分层**(自上而下,依赖方向:上层依赖下层):

- Layer 0(底层/基础设施):一句话
- Layer 1:
- Layer 2:

**模块**:

- `module_a` —— 职责一句话
- `module_b` —— 职责一句话

**禁止**:

- 跨层调用
- Layer X 直接访问 Layer Y+2 内部状态

## 3. 硬约束

**DO**:

1. (祈使句,动词开头,可校验)
2. (祈使句,动词开头,可校验)

**DON'T**:

1. (祈使句 + 一行原因)
2. (祈使句 + 一行原因)

## 4. 关键决策

| 日期 | 决策 | 选择 | 原因 | 拒绝的替代 |
|---|---|---|---|---|
| YYYY-MM-DD | 例:持久化方案 | SQLite | 零运维 + 单文件 | Postgres(运维成本) |
|  |  |  |  |  |

## 5. 工作流

- **开发**:步骤
- **测试**:步骤
- **提交**:步骤
- **文档**:步骤

## 6. 元规则

- 总行数 ≤ 150(硬上限,超过必须瘦身;可在头部声明 maxLines 覆盖,大项目可放宽)
- 触发更新:功能完成 / PR 合并 / 会话收尾
- 更新协议:增量 Edit,不全量重写;镜像相邻条目格式
- 边界:用户偏好 → `USER.md`;历史活动 → memory 日志;项目设计 → 本文件
- 自检:每条都祈使句、都可校验、跨章节不重复

## 变更记录

- YYYY-MM-DD：新增/修订/删除 <条目>（原因）
